# 复制成 .env 填好后启动转换器（go run . 或直接运行编译出的程序）。
# 已经设置的环境变量优先于这里的值。
#
# 转换器不存任何密钥：Draw Studio 后台「供应商」的 API Key 直接填上游（中转站 / 厂商）的 Key，
# 每个请求都带着它来，转换器原样拿去调上游。Key 要经网络到达转换器，所以部署时走 HTTPS，
# 或者和 Draw Studio 放在同一台机器 / 同一内网。

# 上游（中转站 / 厂商）的地址。
UPSTREAM_BASE_URL=https://relay.example.com

# 监听地址和端口。
HOST=0.0.0.0
PORT=8787
