# 模型配置参考 · Model Configuration Reference

> 汇总各图片模型在 **后台「模型管理」新增 / 编辑** 时支持的 **质量等级与分辨率**，方便快速、正确地配置模型。

**已收录：**

- [gpt-image-2（OpenAI）](#gpt-image-2)
- [Gemini Nano Banana 系列](#gemini-nano-banana-系列)（`gemini-3.1-flash-image` / `gemini-3-pro-image` / `gemini-3.1-flash-lite-image`；`gemini-2.5-flash-image` 2026-10-02 停服）
- [通义万相 Wan 系列（DashScope）](#通义万相-wan-系列dashscope)（`wan2.7-image-pro` / `wan2.7-image`，**异步端点**）

---

## 后台配置速查：这些信息填到哪里

`gpt-image-2` 走 **`OpenAI /v1/images/generations`** 端点。该端点的模型表单模板是 `openai`（= **宽高比** + `auto`/`low`/`medium`/`high` 质量 + **1K/2K/4K 尺寸档位附加积分**）。用户在生成时选「质量 + 分辨率档位（自动/1K/2K/4K）+ 宽高比」，系统按 **宽高比 × 档位** 自动算出符合 gpt-image-2 约束的具体分辨率（`宽x高`）下发上游；选「自动」档位则直接下发 `size: "auto"`，无需再选比例。在「后台 → 模型管理 → 新增 / 编辑模型」中：

| 本文档里的概念 | 对应后台字段 | 填写说明 |
|---|---|---|
| 质量等级（Quality） | **图片质量选项**（image quality options） | 填入 `auto` `low` `medium` `high` |
| 质量 → 扣费 | **质量扣费映射**（quality credit mapping） | 每个质量等级单独定价；高质量成本更高 |
| 宽高比（Aspect Ratio） | **图片尺寸选项**（image size options） | 填**宽高比字符串**，如 `1:1` `16:9` `9:16` `21:9`，特殊值 `auto` = 模型自动决定；比例不得超过 3:1。`21:9` 约为 2.33:1，在合法范围内，可按业务需要自行添加（系统不会强制追加默认比例） |
| 分辨率档位 → 扣费 | **尺寸档位附加积分**（1K/2K/4K 三个输入框） | 按实际分辨率落入的档位额外加收；留空按 0 算，「自动」不加收 |
| 自定义分辨率 | **允许自定义分辨率** 开关 | 开启后用户可直接填 `宽×高`（前端自动修正为合法值），按落入的 1K/2K/4K 档位收「尺寸档位附加积分」；仅 gpt-image-2 这类任意分辨率模型可开 |
| 参考图数量 | **最大参考图数量** | 每个模型单独填，留空 = 跟随全局默认。gpt-image-2 填 `16`，详见上方「参考图数量上限」一节 |
| 模型标识 | **模型 slug** | 填上游模型名，如 `gpt-image-2` |

> 选择 **端点类型** 时选 `OpenAI /v1/images/generations`，表单的可视化选项编辑器会自动切换为「宽高比 + 质量 + 档位积分」样式（跟随端点，而非供应商）。
>
> **旧版像素尺寸配置仍然有效**：升级前按 `1024x1024` 这类具体像素配置的模型不用改，用户侧会继续显示为固定尺寸列表（按行的附加积分继续生效）；重新保存成宽高比后自动切换为新版「档位 + 比例」选择器。

---

## 参考图数量上限（按模型配置）

各家模型单次能接受的参考图数量差别很大，所以这是**每个模型单独配置**的：
「后台 → 模型管理 → 新增 / 编辑模型 → **最大参考图数量**」。

- **留空** = 跟随「系统设置 → 最大参考图数量（默认）」（出厂 14）。
- **填数字** = 该模型自己的上限，**优先于全局设置**（不是取两者较小值）。
- **填 0** = 该模型不接受参考图。⚠️ 这会**同时禁用该模型的局部重绘（inpaint）**——局部重绘要把原图作为第一张参考图下发，填 0 之后会报「参考图数量超限（最多 0 张）」。支持局部重绘的模型请至少填 1。
- 上限 100。端点本身不支持参考图时，此项不生效（恒为 0）。

配好之后，**用户端的上传数量直接按这个值限制**，不会再出现「让你传满、提交时才被服务端拒绝」的情况。

### 各模型官方上限速查

| 模型 | 模型标识 | 最大参考图 | 官方推荐细分 |
|---|---|---|---|
| **GPT Image 2** | `gpt-image-2` | **16** | — |
| **Nano Banana 2** | `gemini-3.1-flash-image` | **14** | 最多 10 张物体高保真 + 最多 4 张角色一致性 |
| **Nano Banana Pro** | `gemini-3-pro-image` | **14** | 最多 6 张物体高保真 + 最多 5 张角色一致性 + 最多 3 张风格参考 |
| **Nano Banana**（原版，2026-10-02 停服） | `gemini-2.5-flash-image` | **3** | — |
| **通义万相 Wan** | `wan2.7-image-pro` / `wan2.7-image` | **9** | 详见下方万相章节 |

> 「推荐细分」是官方给出的**用途建议**（物体 / 角色 / 风格各自的优化上限），**总数仍受该行的硬上限约束**。系统只按总数限制，不区分用途。

**gpt-image-2 补充**：16 张的上限用于 `/v1/images/edits` 端点及相关参考图工作流；单张需为 PNG / WebP / JPG，通常 < 50 MB，模型会对所有输入参考图做高保真处理。

来源：
- OpenAI 模型页 <https://developers.openai.com/api/docs/models/gpt-image-2>
- OpenAI 图像生成指南 <https://developers.openai.com/api/docs/guides/image-generation>
- Google Cloud / Firebase Gemini 图像模型文档（`Maximum images per prompt`，Nano Banana 2 与 Pro 均为 14）

---

## gpt-image-2

来源：OpenAI 官方文档（截至 2026 年 6 月最新指南）。

### 1. 支持的质量等级（Quality）

| 质量等级 | 英文参数值 | 说明 | 推荐使用场景 |
|----------|------------|------|--------------|
| 低质量   | `low`      | 生成速度最快、成本最低 | 快速草稿、缩略图、原型迭代 |
| 中等质量 | `medium`   | 速度与质量的平衡选项 | 大多数常规使用场景 |
| 高质量   | `high`     | 最高视觉质量 | 最终交付、专业设计、印刷用途 |
| 自动     | `auto`     | 默认值，模型自动选择最优质量 | 推荐大多数情况下使用 |

> **注意**：`quality` 参数仅对 GPT Image 模型（包括 `gpt-image-2`）有效。

### 2. 支持的分辨率（Size / Resolution）

`gpt-image-2` 支持**任意分辨率**（以 `"WIDTHxHEIGHT"` 字符串指定，例如 `"1536x864"`），但必须满足以下**硬性约束**：

**核心约束条件**

- 宽度和高度必须均为 **16 的整数倍**
- 长边与短边比例不得超过 **3:1**（即宽高比介于 1:3 与 3:1 之间）
- 单边最大长度 ≤ **3840 px**
- 总像素数范围：**655,360 ～ 8,294,400** 像素
- **2560×1440 以上**（约 368 万像素）的分辨率属于**实验性功能**，稳定性可能较低

**本系统的下发方式：宽高比 × 档位 → 具体分辨率**

后台只配**宽高比**；用户生成时选 1K/2K/4K 档位（长边基准 1024 / 2048 / 3840），系统会先按比例计算像素尺寸，再自动对齐到 16 的倍数，并按单边、总像素和 3:1 比例上限修正，最终把真实的 `size: "宽x高"` 下发给 OpenAI。**不会把 `16:9`、`21:9` 这样的比例字符串直接发给 OpenAI。**

工作台与 Agent 共用同一套推导和服务端校验：Agent 的 `aspect_ratio + resolution` 会得到与工作台相同的真实 `size`；用户要求“高清”“4K”“最高分辨率”或“超高清”时，Agent 都会使用 4K 档位并选择真正支持 4K 的模型，不支持时明确报错而不会静默降到 2K/1K。前后端算法一致，扣费与实际下发的分辨率对得上。推导结果示例：

| 宽高比 | 1K | 2K | 4K | 备注 |
|---|---|---|---|---|
| `1:1`  | 1024x1024 | 2048x2048 | **2880x2880** | 4K 正方形超总像素预算，自动压到 2880（≈829 万像素上限） |
| `3:2`  | 1024x688 | 2048x1360 | 3520x2352 | 4K 按像素预算自动压缩 |
| `4:3`  | 1024x768 | 2048x1536 | 3312x2496 | 4K 按像素预算自动压缩 |
| `16:9` | **1072x624** | 2048x1152 | 3840x2160 | 1K 的 1024x576 低于像素下限，自动放大 |
| `9:16` | 624x1072 | 1152x2048 | 2160x3840 | 同上，1K 自动放大 |
| `21:9` | 1232x544 | 2048x880 | 3840x1648 | 合法超宽比例；需管理员在该模型的尺寸选项中自行添加 |
| `auto` | — | — | — | 档位选「自动」时直接下发 `size: "auto"`（推荐默认） |

> 具体数值以工作台尺寸选择器里每个比例块下方的实时预览为准。

**自定义分辨率（可选，按模型开关）**

后台模型编辑里打开「**允许自定义分辨率**」后，用户可在尺寸选择器中直接填 `宽×高`。前端会自动把输入修正为合法值（16 的倍数、单边 ≤3840、比例 ≤3:1、总像素在预算内），服务端同样校验，不会把非法尺寸打到上游。扣费按分辨率落入的 1K/2K/4K 档位收「尺寸档位附加积分」（按最长边：>2048 算 4K、>1536 算 2K、其余 1K）。

同一个开关也决定 Agent 能不能用自定义尺寸：开了之后，Agent 的运行时模型清单里该模型会标注「自定义尺寸」，并附上一行由代码常量生成的约束说明；Agent 可通过 `generate_image` 的 `size`（`宽x高`）直接指定分辨率，服务端按同一套规则就近修正后下发，结果里返回实际尺寸。没开开关的模型（包括同端点的 gpt-image-1 / 1.5）Agent 看不到这项能力，传了 `size` 会被明确拒绝而不是静默改成固定比例。

**API 请求示例**

```json
{
  "model": "gpt-image-2",
  "prompt": "...",
  "size": "1536x1024",
  "quality": "high"
}
```

**提示**

- 正方形图像通常生成速度最快。
- 超出约束范围的请求将返回错误。
- 实际生成时间与成本会随 `quality` 和 `size` 显著变化（高分辨率 + 高质量成本最高）。

---

## Gemini Nano Banana 系列

来源：Google Gemini API 官方文档（2026-09）。这些模型都走 **`Gemini Nano Banana generateContent`** 端点（端点模板 `gemini` = 宽高比 + 分辨率档位）。

> ⚠️ **`gemini-2.5-flash-image`（原版 Nano Banana）2026-10-02 停服**，之后调用全部失败。在用的模型请把模型 Key 改成 `gemini-3.1-flash-image`（Nano Banana 2），只要 1K、追求速度可改 `gemini-3.1-flash-lite-image`。带 `-preview` 的旧名（如 `gemini-3.1-flash-image-preview`、`gemini-3-pro-image-preview`）已于 2026-06-25 停服，同样改成不带 `-preview` 的正式名。

### 模型对比总览

| 模型名称 | Gemini API 标识符 | 支持的分辨率 (image_size) | 支持的比例数量 | 备注 |
|---|---|---|---|---|
| **Nano Banana 2**（推荐） | `gemini-3.1-flash-image` | `"512"`, `"1K"`, `"2K"`, `"4K"` | **14 种** | 支持最全比例，推荐大多数场景 |
| **Nano Banana Pro** | `gemini-3-pro-image` | `"1K"`, `"2K"`, `"4K"` | **10 种** | 专业级，最高支持 4K |
| **Nano Banana 2 Lite** | `gemini-3.1-flash-lite-image` | 仅 `"1K"` | 10 种 | 速度优先，只出 1K |
| **Nano Banana**（2026-10-02 停服） | `gemini-2.5-flash-image` | 仅 `"1K"` | 10 种 | 停服前请换模型 |

> **默认值**：未指定时 `image_size` 默认 `"1K"`。

### 支持的宽高比（Aspect Ratio）

**Nano Banana 2（`gemini-3.1-flash-image`）—— 最全，14 种**

```text
"1:1", "1:4", "1:8", "2:3", "3:2", "3:4",
"4:1", "4:3", "4:5", "5:4", "8:1",
"9:16", "16:9", "21:9"
```

**Nano Banana Pro（`gemini-3-pro-image`）—— 10 种**

```text
"1:1", "2:3", "3:2", "3:4", "4:3", "4:5", "5:4",
"9:16", "16:9", "21:9"
```

**Nano Banana 2 Lite（`gemini-3.1-flash-lite-image`）/ Nano Banana（`gemini-2.5-flash-image`）—— 10 种**

```text
"1:1", "2:3", "3:2", "3:4", "4:3", "4:5", "5:4",
"9:16", "16:9", "21:9"
```
### 支持的分辨率档位（Image Size）

| 模型 | 支持值 | 说明 |
|---|---|---|
| **Nano Banana 2** | `"512"`, `"1K"`, `"2K"`, `"4K"` | 唯一支持 512 的型号 |
| **Nano Banana 2 Lite** / **Nano Banana** | `"1K"` | 固定 1K（约 1024px 级别），无其他选项 |
| **Nano Banana Pro** | `"1K"`, `"2K"`, `"4K"` | 高分辨率输出 |

> - 更高分辨率（2K/4K）会增加生成时间和 Token 消耗。
> - 实际像素尺寸按所选宽高比自动计算（例如 16:9 的 2K 约为 2752×1536）。

### 在本系统后台如何配置

gemini 模板下，**宽高比** 配在「图片尺寸选项」、**分辨率档位** 配在「图片质量选项」：

| 概念 | 对应后台字段 | 填写说明 |
|---|---|---|
| 端点类型 | **端点选择** | 选 `Gemini Nano Banana generateContent` |
| 模型 slug | **模型 slug** | 填 API 标识符，如 `gemini-3.1-flash-image` |
| 宽高比（Aspect Ratio） | **图片尺寸选项**（image size options） | 填**宽高比字符串**，如 `1:1` `16:9` `9:16` `21:9`；原样作为 `imageConfig.aspectRatio` 下发，任意 `N:M` 均可透传 |
| 分辨率档位 512/1K/2K/4K | **图片质量选项**（image quality options） | 直接用 `1K`/`2K`/`4K`（Nano Banana 2 另可填 `512`）作质量值最直观，原样下发为 `imageConfig.imageSize`；大小写都认，一律按大写 K 发出；也兼容 `high`→4K、`medium`→2K、`low`/`auto`→1K |
| 比例 → 扣费 | **图片尺寸扣费映射** | 按宽高比定价 |
| 档位 → 扣费 | **质量扣费映射** | 按 1K/2K/4K 定价（高分辨率成本更高） |

> 字段映射在 `backend/internal/ai/registry.go` + `backend/internal/imagegen/adapters.go` 硬编码：`imageConfig.aspectRatio = getAspectRatio(尺寸选项)`、`imageConfig.imageSize = geminiImageSizeParam(质量选项)`。后台无需关心 wire 字段名。
>
> 「默认参数」里写 `generationConfig`（如 `{"generationConfig":{"temperature":1}}`）会与请求自带的合并，不会冲掉用户选的比例和档位。

**注意事项（与上游能力的差异）**

- ⚠️ **分辨率档位随质量始终下发**：给只支持 `1K` 的 Lite / 原版配 `2K`/`4K`，或给 Pro 配 `512`，上游会报错 —— 按各模型实际支持范围设置质量选项。
- ⚠️ **极端比例按模型区分**：`1:8`/`8:1`/`1:4`/`4:1` 仅 Nano Banana 2 支持；给另外两个模型配这些比例可能报错。

---

## 通义万相 Wan 系列（DashScope）

来源：阿里云百炼「万相 2.7 图像生成与编辑」官方文档。走 **`DashScope 通义万相 image (异步)`** 端点（`AuthPreset: dashscope`，路径 `/api/v1/services/aigc/image-generation/generation`）。

> **⚠️ 异步端点**：与 OpenAI / Gemini「一次请求拿结果」不同，本端点上游是**异步任务流**——后端先创建任务（`X-DashScope-Async: enable`）拿 `task_id`，再轮询 `GET /api/v1/tasks/{task_id}` 至 `SUCCEEDED`，下载结果图（URL 有效期 24h）后转存到对象存储。这些都由后端适配器完成，**后台配置方式与同步端点完全一致**，无需特殊处理。

端点模板是 **`gemini`**（= 宽高比 + 1K/2K/4K 分级），所以后台模型表单与 Gemini 系列长得一样。

### 模型对比总览

| 模型标识（slug / model_key） | 简介 | 支持的分辨率档位 | 4K 支持 |
|---|---|---|---|
| **`wan2.7-image-pro`** | 专业版，质量更高 | `1K` / `2K`（默认）/ `4K` | ✅ **仅文生图（无输入图、非组图）** 支持 4K；图像编辑最高 2K |
| **`wan2.7-image`** | 速度更快 | `1K` / `2K`（默认） | ❌ 不支持 4K |

> 各档位总像素：**1K ≈ 1024×1024、2K ≈ 2048×2048、4K ≈ 4096×4096**。

### 分辨率与宽高比

`size` 参数支持两种**互斥**写法，本系统适配器会自动选择：

- **档位法（推荐）**：`1K` / `2K` / `4K`。文生图输出为**正方形**；有输入图时输出**自动匹配输入图的宽高比**并缩放到该档位。
- **像素法**：`宽*高`（**星号分隔**，如 `2048*1152`）。文生图总像素 `[768*768, 4096*4096]`、其他场景 `[768*768, 2048*2048]`，宽高比范围均为 `[1:8, 8:1]`。

本系统映射规则(`backend/internal/imagegen/dashscope.go`)：

- **质量选项**（1K/2K/4K）→ 决定 `size` 档位；
- **宽高比选项**(如 `16:9`)→ 仅文生图生效：适配器按「档位 + 比例」算出像素 `宽*高` 下发；方形 / `auto` 直接用档位；**极端比例在低档位下若低于 768×768 会自动回退方形档位**；
- **图像编辑**（带参考图）：忽略所配比例(上游按输入图比例缩放)，且 **4K 自动降为 2K**(编辑场景上游不支持 4K)；
- 实际输出尺寸以上游返回的 `usage.size` 为准，存库尺寸准确。

### 输入图片（图像编辑 / 多图参考）

- 端点 `SupportsReferenceImages = true`：支持传入参考图做**图像编辑 / 多图参考**(0–9 张，本系统按「编辑」处理，参考图经 SSRF 校验后以 base64 内联下发)。**端点硬上限 9 张**：模型的「最大参考图数量」留空或填得更大，也按 9 张限制。
- **提示词上限 5000 字**：上游对超出部分静默截断，本系统在提交前拦下超长提示词，不扣费。
- **不支持**红蒙版 inpaint(`SupportsInpaint = false`，万相用 bbox 框选式编辑，非蒙版)与 outpaint。
- 上游图片限制：JPEG/PNG(无透明通道)/BMP/WEBP，宽高 `[240, 8000]`、比例 `[1:8, 8:1]`、≤20MB。

### 在本系统后台如何配置

| 概念 | 对应后台字段 | 填写说明 |
|---|---|---|
| 供应商 | **供应商管理** | 新建供应商：Base URL 推荐填业务空间地址 `https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com`（阿里云推荐生产环境使用；老域名 `https://dashscope.aliyuncs.com` 目前仍可用），API Key = 百炼 `sk-xxx`，地址、模型与 Key 须同一地域。`/api/v1` 由系统自动补上 |
| 端点类型 | **端点选择** | 选 `DashScope 通义万相 image (异步)` |
| 模型 slug | **模型 slug / 模型 Key** | 填 `wan2.7-image-pro` 或 `wan2.7-image` |
| 分辨率档位 1K/2K/4K | **图片质量选项**（image quality options） | 用 `1K`/`2K`/`4K` 作质量值；也兼容 `high`→4K、`medium`→2K、`low`/`auto`→1K |
| 宽高比 | **图片尺寸选项**（image size options） | 填宽高比字符串如 `1:1` `16:9` `9:16`(仅影响文生图)；也可直接填像素 `1664x928`(系统自动转成万相的 `1664*928`) |
| 档位 → 扣费 | **质量扣费映射** | 按 1K/2K/4K 定价(高分辨率成本更高) |
| 比例 → 扣费 | **图片尺寸扣费映射** | 按宽高比定价 |
| `watermark` / `thinking_mode` / `seed` 等 | **高级默认参数**（defaultParams JSON） | 可选；原样并入 `parameters`(用户所选 `size`/`n` 优先) |

### ✍️ 选项栏到底填什么(复制即用)

**① 图片质量选项(= 分辨率档位)** —— 填这几个值:

```text
1K
2K
4K
```

> `4K` **只有 `wan2.7-image-pro` 的文生图**支持;配 `wan2.7-image` 时**只填 `1K` `2K`**,别填 `4K`。

**② 图片尺寸选项(= 宽高比)** —— 推荐填这一组常用比例(横/竖/方各覆盖):

```text
1:1
16:9
9:16
4:3
3:4
3:2
2:3
4:5
5:4
21:9
```

**填写规则:**

- 比例写成 `宽:高` 字符串,**任意 `N:M` 都能透传**,只要落在万相允许的 **`1:8` ～ `8:1`** 范围内(比如还想加 `2:1` `1:2` `3:1` `1:3` 都行)。
- 想要**精确像素**:把某一项直接填成 `宽x高`(如 `1664x928`),系统会自动转成万相的 `1664*928` 下发。注意**总像素**别超上限——**文生图(pro)≤ 4096×4096、编辑/组图及 `wan2.7-image` ≤ 2048×2048**,且比例仍需在 `1:8`～`8:1` 内。
- ⚠️ **宽高比/自定义像素只对「文生图」生效。** 走**图像编辑(带参考图)**时,输出比例由**输入图自动决定**(缩放到所选档位),这里配的比例会被忽略。
- 每个尺寸选项可在「**图片尺寸扣费映射**」里单独定价(比例本身不影响万相计费,万相按出图张数计费,但你可以借此对不同比例做差异化收费)。

> 选项填好后,用户在生成时就能从下拉里选「分辨率档位 + 宽高比」两项;系统把它们合成万相的 `parameters.size`(档位法或像素法)下发。

**注意事项**

- ⚠️ **本系统只取首图、固定 `n=1`**：不支持组图(`enable_sequential`)多图输出——即便上游返回多张也只采用第一张。
- ⚠️ **4K 仅 `wan2.7-image-pro` 的文生图**：给 `wan2.7-image` 配 4K、或在图像编辑中用 4K 都会被降级 / 报错，按模型与场景设置质量档位。
- ⚠️ **`thinking_mode` 默认开启**：仅在文生图(无输入图、非组图)时生效，会增加耗时但提升质量；可在默认参数里关。
- ⚠️ **异步耗时**：受上游排队影响，单次可能数十秒。后端轮询预算由 `generation_upstream_timeout_seconds`(默认 600s)控制，轮询期间会心跳刷新任务状态，避免被「超时回收」误判。

---

## 通用协议（协议转换器）：接上面没有的任何上游

内置端点接不上的上游（中转站的私有格式、新厂商），不用等新版本：你自己部署一个**协议转换器**小服务，它按我们的「通用生成协议」接收请求，再翻译成上游的格式。新增模型时「API 适配」选 **通用协议 · 图片** 或 **通用协议 · 视频**。

**转换器不用手写。** 模型表单里有一句「帮我安装这个 skills：…」，复制发给你自己的 Agent（Claude Code / Codex 都可以），再把上游 API 文档丢给它，它会先问你习惯用什么语言（Go / Node.js / Python 都可以），再写出转换器和自查测试（模拟上游，本机就能跑，不需要 Docker），并告诉你怎么启动。技能包在公开部署仓 `skills/draw-protocol-converter/`（`SKILL.md`、协议全文 `protocol.md`、一个 Go 写的完整示例）。

上线步骤：

1. **部署转换器**：放到 Draw Studio 能访问到的机器上，复制 `.env.example` 为 `.env` 填好后直接运行（也可以按 Agent 给的 Dockerfile 用 `docker run --env-file .env`）。转换器只需要配上游地址，**不存任何 Key**。上游 Key 会经网络到达转换器，所以转换器要走 HTTPS，或者和 Draw Studio 放在同一台机器 / 同一内网。前面有 nginx 的话读超时不小于 600 秒。
2. **新增供应商**：Base URL 填转换器地址，API Key 直接填**上游的 Key**（转换器原样透传给上游）。上游按分组 / 账号发 Key 的（比如图片和视频要不同分组的 Key），一个 Key 建一个供应商，Base URL 都填同一个转换器。
3. **新增模型**：API 适配选「通用协议 · 图片 / 视频」，供应商选上一步那个，点 **「读取能力」**，在要用的模型旁点 **「使用」**：模型 Key、能力开关和选项会自动填好（转换器给了建议定价的也会预填）。核对「带 Key 下载」列出的是上游自己的域名，填好价格后保存。
4. **出一次图 / 一条片**验证。失败会自动退积分，原始报错在「出图任务 / 视频任务」里。

| 概念 | 对应后台字段 | 说明 |
|---|---|---|
| 转换器地址 + 上游 Key | **供应商** | 地址带不带 `/v1` 都可以；Key 是上游的，转换器原样透传 |
| 带 Key 下载 | 「读取能力」后显示在模型声明下 | 结果链接落在这些地址（上游自己的域名）上时，本站下载会带上供应商 Key；其它地址一律匿名下载，Key 不会外发 |
| 能力声明 | 参考图 / 局部重绘 / 扩图 / 图层拆分 / 自定义分辨率 / 选项模板；视频的时长、比例、分辨率、素材槽位 | 以「读取能力」拿到的声明为准，没声明的能力开不了；视频的这些项只读展示 |
| 建议定价 | 基础积分 / 档位附加积分；视频每档积分 | 新建时自动预填，编辑时可「按声明重填定价」；扣费永远只读表单里的数字 |
| 高级默认参数 | **高级默认参数** JSON | 原样放进请求的 `params` 交给转换器（反向提示词、水印开关之类） |

注意：

- 转换器改了能力（加了比例、换了上限）或换了上游地址后，要回模型里重新「读取能力」并保存，已保存的模型不会自动跟着变（「带 Key 下载」的地址也存在模型里）。
- 转换器的错误分类决定用户看到的提示和「模型状态」页是否变红；原始报错只进任务记录，不给终端用户看。
- 2.105.0 之前的「自定义端点（脚本）」已下线。用过它的模型会在模型列表里标红「端点已下线」，需要改用通用协议：原来的 `adapter.js` 可以直接交给 Agent 当参考，让它写成转换器。
